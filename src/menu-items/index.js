import dashboard from './dashboard';
import master from './master';

// ==============================|| MENU ITEMS ||============================== //

const menuItems = {
  // items: [dashboard,pages, master, utilities, other],
  items: [dashboard, master],
};

export default menuItems;
